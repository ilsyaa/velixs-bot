module.exports = {
    name : "antilink-group",
    description : "Antilink group. ketika ada link di group akan otomatis di hapus.",
    run : async({ m, sock }) => {
        return next = true;
    }
}